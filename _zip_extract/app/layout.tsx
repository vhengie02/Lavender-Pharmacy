import type { Metadata, Viewport } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter'
});

const playfair = Playfair_Display({ 
  subsets: ["latin"],
  variable: '--font-playfair'
});

export const metadata: Metadata = {
  title: 'Lavender Pharmacy | Your Trusted Healthcare Partner',
  description: 'Lavender Pharmacy offers quality medications, expert consultations, and personalized care. Your health and wellness are our priority.',
  keywords: ['pharmacy', 'medications', 'healthcare', 'prescriptions', 'wellness'],
}

export const viewport: Viewport = {
  themeColor: '#B57EDC',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable} bg-background`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
